{
    "version": 2,
    "builds": [{
        "src": "select_class/wsgi.py",
        "use": "@vercel/python"
    }],
    "routes": [
        {
            "src": "/static/(.*)",
            "dest": "/static/$1"
        },
        {
            "src": "/media/(.*)",
            "dest": "/media/$1"
        },
        {
            "src": "/(.*)",
            "dest": "select_class/wsgi.py"
        }
    ],
    "env": {
        "PYTHONPATH": "."
    }
}
